---
name: review-gate
description: PR、User Story、PLAN、実装成果を、現在の契約と到達可能な影響範囲に限定して独立レビューし、根拠付きのGO／NO-GOを一度に判定する。Use when asked to review a PR or Gate, perform an independent review, re-review a changed result, or decide whether an artifact may advance. Do not use for implementation, open-ended improvement, or general hardening unless explicitly requested.
---

# Review Gate

現在の成果を次工程へ渡してよいかを、契約と直接証拠で判定する。

一つの指摘が見つかると、Gateの結論は早く決まることがある。しかし、**結論が決まることと、探索が完了することは別である。** 指摘の発見を探索の終了条件にしない。

レビュー状態は、`準備中 → 適用範囲確定済み → 探索中 → 探索完了済み → 候補整理済み → 最終確認済み → 判定済み`の順に進める。各状態は、対応するGateの完了条件をすべて満たした場合だけ進める。一件の指摘は状態遷移を発生させない。`候補整理済み`より前に許される外部報告は、必要な場合の`HOLD（レビュー継続中）`だけである。修正指示、確定したGO／NO-GO、共有成果物の編集は行わない。

依頼者が最初の一件、smoke review、特定反例だけを明示的に求めた場合は、その限定範囲で終了してよい。ただし結果を`限定レビュー`と明記し、GateのGO／NO-GO、探索完了、未確認0件、全体に他の指摘がないことを主張しない。限定レビューの結果だけで、Story、PLAN、実装のGateや状態を更新しない。

独立レビューまたはGateレビューでは、作成・実装過程の結論、既知の指摘、想定修正を引き継がない新しい文脈の担当を使う。独立した担当を利用できない場合は、その事実を報告し、独立Gateを通過したことにしない。

## 1. レビュー契約を固定する

深い探索を始める前に、一時的なReview Briefへ次を記載する。

- Yes／Noで答えるGate固有の判定質問
- 対象成果と、実行時に確認した同一の成果状態
- 成立させる条件ID、公開契約、変更経路が影響し得る全体不変条件
- 対象範囲、対象外、通常導線、明示済みの例外導線
- 意味を変更した箇所と、変更された公開状態・出力・外部作用
- 適用される指示、一次情報、関連成果物
- 後工程が所有する責務と、現在のGateが渡す名前付きのhandoff契約
- 条件ID、意味を変更したsurface、影響し得る全体不変条件、handoffから導いたcoverage seed。各seedには、`must-applicable`、`classify-only`、`handoff-pair`のcoverage obligationと、一次情報から導いた確認case IDを付ける

Review Briefとcoverage seedは、main Codexが独立reviewerへ渡す前に固定する。reviewerは契約、seed、handoffを自分で追加・削除しない。不足を見つけた場合は`HOLD（Review Brief再固定待ち）`としてmainへ返す。mainが一次情報から再固定した後に、適用範囲Gateを最初から通す。

Review Briefへ作成者の結論、既知の指摘、望む修正を混ぜない。対象の同一性はレビュー中に確認するが、PLANやStoryへbranch、SHA、content digestを記録しない。

## 2. 適用範囲Gateを通す

不具合を探し始める前に、一時的な「適用性判定一覧」を作る。境界を判断するための浅い探索はよいが、指摘探しや深い反例探索はまだ行わない。

入口候補は、次の二方向から独立に導く。

1. 条件IDと利用者が観測する結果から、生成側へ遡る。
2. 意味を変更した箇所、公開契約、共有状態、外部作用から、現在の成果が到達し得る既存の利用側と、Review Briefで固定したhandoff先の観測結果へ進む。

両方向の和集合を入口候補にする。一方にしか現れない経路は、漏れの可能性がある独立した候補にする。各入口候補をReview Briefのcoverage seedへ一対一で結び付け、すべてのseedを少なくとも一つの候補で覆う。検索で見つからないことだけを、利用側や状態が存在しない根拠にしない。

入口候補は、次のいずれかへ分類する。

- `適用`: 条件ID、現在のGate質問、現在の成果が所有する公開契約、または変更経路が影響し得る全体不変条件のいずれかを起点にできる。現在の成果から観測結果まで到達可能な因果経路があり、破られ得る既存契約と探索を止める境界を示せる。
- `明示済み後工程`: Review Briefへ先に記載された名前付きGate、owner、handoff契約へ現在のGateが実際に値または状態を渡す。現在のレビューではhandoff面までを確認し、後工程の内部実装へ入らない。
- `非適用`: 現在の契約、到達経路、明示済みhandoffのいずれにも結び付かない。改善案、将来用途、一般的なbest practice、別Storyだけを根拠にした候補はここへ置く。
- `未確認`: 適用性を判断するために必要な一次情報を取得できない。

名前付きGateとownerが存在するだけでは、`明示済み後工程`にしない。現在のhandoff契約まで示せない候補を、後続作業として残さない。`契約判断待ち`は適用性分類ではなく、現在の既存契約同士が衝突してGate質問へ答えられない場合にだけ、指摘採用Gateで使う。

契約上必要な呼出し、配線、状態遷移が実装に存在しない場合、その欠落自体が到達可能な反例である。「経路が存在しない」ことを理由に`非適用`へ落とさない。

適用範囲Gateは、次をすべて満たした場合だけ通過する。

- Gate質問、対象成果、現在の契約、対象外が固定されている
- 二方向から導いた入口候補に、未分類と`未確認`がない
- Review Briefのcoverage seedがすべて入口候補へ対応している
- 各seedがReview Briefで固定したcoverage obligationを満たしている。条件と全体不変条件は`must-applicable`、意味を変更したsurfaceは適用を決め打ちしない`classify-only`、handoffは同じseed・handoff・経路に`適用`と`明示済み後工程`を持つ`handoff-pair`とする
- すべての`適用`候補に、起点となる契約、到達可能な因果経路、探索を止める境界、探索前に固定した確認case IDがある
- すべての`明示済み後工程`候補に、Review Briefで事前に固定したGate、owner、handoff契約がある
- すべての`明示済み後工程`候補に、現在Gateのhandoff面を確認する`適用`候補がある
- すべての`非適用`候補に、現在の契約または到達経路へ入らない根拠がある

一つでも満たさない場合は`HOLD（適用範囲未確定）`とし、`探索中`へ進まない。通過時に、各分類の候補IDと根拠を「適用範囲Gate完了証明」として固定する。

限定レビューを除くGateレビューでは、[Review Gate manifest](references/manifest-schema.md)を作る。main Codex自身が、固定したReview Briefとともにvalidatorを`--stage scope`で実行する。reviewerの成功報告だけを証明にしない。終了codeが0でない場合は`適用範囲確定済み`へ進まない。成功したscope manifestはbaselineとして保持する。いずれも一時成果物とし、PLANやStoryへ保存しない。

適用範囲Gateの`適用`候補だけから、一時的な「レビュー確認一覧」を作る。表、箇条書き、構造化データのどれでもよい。形式ではなく、各確認単位を識別できることを必須とする。

各単位には次を持たせる。

- ID
- 適用範囲Gateで固定した確認case ID。一つのcaseを複数の確認単位へ複製しない
- 守る契約
- 到達可能な開始状態、操作、必要な割込み順序
- `契約 → 生成側 → 状態・変換・外部作用 → 利用側 → 観測結果`の因果経路
- 探索を止める境界と、その境界の意味が変わっていない根拠
- 合否を観測する方法、証拠の取得方法、その証拠が保証する範囲
- 状態。開始時は`未着手`とする

レビュー確認一覧は探索開始時に固定し、その後は黙って削らない。探索中に新しい入口候補を見つけた場合は、その候補を深掘りせず適用範囲Gateへ戻す。直前のscope baselineを`--previous-scope-baseline`へ渡してscope stageを再実行し、成功した新baselineへ置き換えてから探索を再開する。`適用`を再分類する場合は、main Codexが直前の確認単位、状態、証拠、担当をobservation checkpointとして先に固定し、`--observation-checkpoint`で照合する。再分類するscopeの全確認caseをcheckpointへ一度ずつ残し、取得済みの全単位をtombstoneへ移す。candidate stageへ候補を直接追加しない。`適用`なら理由とともに確認単位を追加し、それ以外なら境界で止める。広さを有限にできない場合は、独立して判定できる縦の範囲へ分割する。範囲を決められないままGOにしない。

変更の性質に応じた確認方法が必要な場合は、[確認方法の選び方](references/technique-routing.md)を読む。そこで選んだ確認事項も、探索前にレビュー確認一覧へ追加する。

## 3. 指摘と探索を分離する

レビュー確認一覧の各単位を調べ、状態を次のいずれかへ更新する。

- `成立`: 現在の成果で契約を満たす直接証拠がある
- `違反`: 到達可能な反例と、破られる契約を示せる
- `未確認`: 必要な証拠を取得できない

探索中に`適用`の根拠が反証された単位は、深掘りを止めて適用範囲Gateへ戻す。適用性判定一覧を`非適用`へ更新し、理由を残してレビュー確認一覧から外す。黙って削除しない。

指摘候補はレビュー確認一覧と分けた一時的な候補一覧へ追加し、対応する確認単位IDを付ける。候補を見つけても、他の単位を`成立`にしない。

- 一件目の指摘で探索を止めない。
- 探索中に確定した修正指示、最終NO-GO、修正依頼を送らない。
- 途中経過を伝える場合は「暫定・探索継続中」と明記する。
- レビュー担当を分けた場合は、全員の完了を待つ。
- 少なくとも一つの確認は、生成側から利用側と観測結果まで横断する。
- 新しい入口候補が現れた場合は、指摘候補として採用する前に適用範囲Gateへ戻す。

レビュー担当の`failed`、`interrupted`、`timed_out`、応答待ちは完了に数えない。担当が続行できない場合は、残る確認単位を別の`completed`担当へ明示的に移し、元の割当を`reassigned（引継ぎ済み）`として残すか、`未確認`として停止理由を残す。

テスト件数、coverage率、確認したファイル数、担当領域名、「全体を読んだ」という自己申告は、確認単位の証拠にしない。各証拠が、どのシナリオをどの観測方法で判定するかを示す。

証拠は、`実行して観測`、`静的に確認`、`既存証拠を照合`のいずれかを明示する。実行していないテスト、command、反例を「成功した」「実行結果」と書かない。静的確認で反例が確定する場合は、実行結果ではなく、該当する分岐と状態変化を根拠として記録する。既存証拠を使う場合は、その証拠が作られた対象と現在の対象で前提が変わっていないことを示す。

## 4. 探索完了Gateを通す

次をすべて満たすまで、探索完了と報告しない。

- 適用範囲Gateで固定したすべての確認caseに、ちょうど一つの確認単位があり、`未着手`が0件である
- すべての単位に、`成立`、`違反`、`未確認`のいずれかと根拠がある
- 上流方向と下流方向の探索が、根拠のある境界で閉じている
- 適用された追加確認方法を実施している
- 対象成果が探索開始時から変わっていない
- 必須の確認単位を割り当てられた担当が`completed`、または全単位を`completed`担当へ移した`reassigned（引継ぎ済み）`である
- すべての指摘候補が確認単位へ結び付いている
- 適用範囲Gate完了証明が、探索終了時点でも成立している

ここでいう`未確認`は有効な最終状態だが、GOの根拠にはならない。必要な一次情報を取得できない、対象が変わった、権限や外部状態が必要である、といった実際の停止理由を報告する。

これらを満たしたら、scope baselineから探索結果manifestを作り、validatorを`--stage discovery`で実行する。終了codeが0でない場合は`探索完了済み`へ進まない。成功したmanifestをdiscovery baselineとして固定し、指摘採用中に確認単位、状態、証拠、担当、scope履歴を変更しない。

この「探索完了Gate」が、一件目の指摘で止まることを機械的に防ぐ。コードのblockerが一件見つかった時点でGateはNO-GO見込みになるが、それだけでは探索完了にならない。**Gate判定が既知でも、レビュー確認一覧が閉じるまでレビューを続ける。**

複数のレビュー担当を使う場合も、main Codexは各担当の報告を並べるだけで終えない。レビュー確認一覧の和集合、上流と下流の境界、単位間の隙間を自分で照合する。候補を事実誤認として棄却する場合は、一次情報を自分で確認し、必要なら候補を出した担当へ一度だけ事実を確認する。

## 5. 指摘採用Gateを通す

`探索完了済み`になった後で、各候補を次の三つの軸で別々に判定する。

1. **指摘として成立するか**: 到達可能なシナリオ、破られる名前付き契約、一次根拠があるか。
2. **現在のGateを止めるか**: 現在の成果または明示済みの全体不変条件の成立可否を変えるか。
3. **現在の作業で直せるか**: 依頼者の依頼と権限内で、利用者契約や製品判断を増減させず修正できるか。

結果を`fix-here`、`later-gate`、`契約判断待ち`、`非適用`へ振り分ける。呼び出し側が、より具体的な振り分け先を定義している場合はそれに従う。

- `fix-here`: `適用`の確認単位に結び付き、現在のGateが所有する契約を破り、現在の依頼と権限内で契約を変えずに修正できる。
- `later-gate`: 適用範囲Gateで`明示済み後工程`に分類済みのhandoff契約と一致する。レビュー中に新しい後続作業を作らない。
- `契約判断待ち`: 現在のGateに適用される既存契約同士が衝突し、どちらを正とするか決めないとGate質問へ答えられない。将来用途、改善案、owner不在、要件の追加提案には使わない。
- `非適用`: 上のいずれにも該当しない。指摘、修正、後続作業へ昇格させない。

候補の判定中に、適用範囲Gateにない到達経路またはhandoffが必要になった場合は、候補を採用せず適用範囲Gateへ戻す。

同じ根本原因の候補をまとめ、由来を残す。レビューだけを依頼された場合は成果物を変更しない。修正担当へ渡す場合も、探索完了後に採用した全件を一つの指示へまとめる。

指摘採用Gateは、すべての候補に確認単位ID、三つの判定、振り分けと根拠があり、未分類が0件の場合だけ通過する。`later-gate`は適用範囲Gateのhandoff候補と照合し、`契約判断待ち`は既存契約の衝突を示す。これらを満たした場合だけ`候補整理済み`へ進む。

Gateレビューではdiscovery baselineから最終manifestを作り、mainが固定したReview Brief、scope baseline、discovery baselineを別入力としてvalidatorを`--stage candidate`で実行する。candidate stageで追加できるのは候補の振り分けだけであり、scope、確認単位、状態、証拠、担当、scope履歴は変更できない。終了codeが0でない場合は指摘採用Gateを通過させない。validatorは構造上の欠落だけを検査するため、main Codexは契約、到達経路、探索境界、再分類、証拠の内容も一次情報で確認する。

候補整理の完了時に、適用範囲Gate、探索完了Gate、指摘採用Gateの完了証明、対象成果、確認単位の全ID、各単位の状態・証拠の取得方法・保証範囲・探索境界、レビュー担当の状態、候補の採否と振り分けを一つの完了証明として固定する。件数の内訳は照合用であり、各単位の根拠を代用しない。三つのGateがすべて通っていない結果を、修正担当へ送らない。

## 6. 修正後の最終成果を確認する

探索後に成果の意味が変わった場合は、Review Briefを更新し、条件側から上流へ、変更側から下流へ、確認単位を再び導く。既知の単位だけを開き直さず、新しい利用側、状態、外部作用が増えていないか確認する。Gate質問または契約集合が変わった場合は、レビュー確認一覧全体を作り直す。意味が変わっていない場合は、同じレビューを繰り返さない。

次の場合だけ、閉じた確認単位を再び開く。

- 対象成果が変わった
- 新しい到達経路または利用側が判明した
- `非適用`の根拠が反証された
- 修正によって証拠の前提が変わった

単なる改善案、別Story、後続Gate、将来の一般的な堅牢化では再開しない。同じ確認の次元から指摘が小分けで続く場合は、局所修正を止め、確認単位の切り方、状態遷移、公開契約、設計境界を見直す。

同じ対象成果から最終報告後に新しい適用内の指摘が見つかった場合は、追加指摘だけを追送しない。完了証明が不完全だったものとして失効させ、Review Briefと二方向の確認範囲を再導出する。欠けていた確認単位を追加し、影響する単位の探索と候補統合をやり直してから一つの報告へまとめる。

## 7. Gateを判定する

最終報告は、次の順で短くまとめる。

1. Gate固有の質問へのGO／NO-GO
2. 適用範囲Gate、探索完了Gate、指摘採用Gateの通過結果
3. 採用した指摘。なければ「なし」
4. 適用性判定一覧とレビュー確認一覧の件数・内訳
5. 明示済み後工程が所有するhandoff責務
6. 合否に必要だが未確認の事項と、同じ範囲に残るリスク

GOにできるのは、最終成果について未解決のblockerと合否に必要な`未確認`がなく、Gate質問へ根拠付きでYesと答えられる場合だけである。「問題なし」やテスト成功だけをGate通過の根拠にしない。
