interface:
  display_name: "Worker・Model 選定"
  short_description: "作業ごとに worker と subagent model を選び、安全に委任する"
  default_prompt: "$cursor-agent-delegate を使って作業ごとの worker、model、reasoning effort を選定し、安全に委任できる部分だけを実行・検収してください。"
