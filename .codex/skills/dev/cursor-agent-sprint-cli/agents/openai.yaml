interface:
  display_name: "Cursor Agent Sprint CLI（CLI版）"
  short_description: "独立性と検証oracleでCursor CLI委任を実行"
  default_prompt: "$cursor-agent-sprint-cli を使って、判断がfixedまたはboundedで、独立・検証・局所棄却できるtaskをCursor CLIへ委任してください。"
