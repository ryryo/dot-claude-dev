# <作業内容を表すタイトル>

このファイルを実行計画と進捗の正とする。main Codexだけが進捗と完了判断を更新する。

## 目的

<実装後に誰が何をでき、どの振る舞いで確認できるか>

## 前提・対象範囲

- <計画に影響するリポジトリ上の事実>
- <対象に含めるもの>
- <変更しないもの、守る制約>

## 進捗

- [ ] <観測可能な成果単位の作業>
- [ ] <必要な検証を実行し、結果を確認する>
- [ ] <Cursor sprintを使う場合: sprint分割、依存、障壁、main Codexの検収条件を確定する>
- [ ] <Cursor sprintを使う場合: Sprint S1の目的を実行し、main Codexが検収する>
- [ ] <必要なsprint数に合わせてS2以降を追加する。委任しない場合はsprint項目を削除する>

## 完了条件

- <ユーザーまたは呼び出し元から観測できる完了条件>
- `<検証コマンド>`が成功し、<期待する結果>になる

## 委任計画

<Cursor sprintを使う場合だけ残す。worker内部の作業は書かず、使わない場合は見出しごと削除する。>

### Sprint S1: <sprintの目的>

- 元の進捗項目: <対応するチェック項目>
- 依存・障壁: <先行sprint、ユーザー操作、外部状態。なければ「なし」>
- main Codex担当: <契約、統合、本番操作など>
- Cursor委任候補: <分離可能な局所作業>
- 禁止事項: <workerが変更・実行してはいけない範囲>
- main Codexの検収条件: <main Codexが確認する差分、対象範囲、検証結果>
- 次への引き継ぎ: <次sprintへ渡す確定事項。最終sprintなら「なし」>
- Sprintディレクトリ: `.codex/tmp/<実行時に作成したディレクトリ>/`

<必要なsprint数に合わせてS2以降を追加する。>

## 補足

<後続作業に必要な判断、発見事項、リスク、結果がある場合だけ残す。不要なら見出しごと削除する。>
