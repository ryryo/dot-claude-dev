interface:
  display_name: "Codex Luna Task Split"
  short_description: "判断状態と検証可能性でSolとLunaの作業を安全に分担"
  default_prompt: "Use $codex-luna-task-split to split mixed work by decision state, independence, side effects, verification strength, and review benefit without creating Sprint artifacts."
policy:
  allow_implicit_invocation: true
