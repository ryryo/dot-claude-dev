# 仕様書テンプレート

仕様書 `docs/PLAN/{YYMMDD}_{slug}.md` は以下の構成で作成する。

---

## 必須セクション

```markdown
# {タイトル}

## Gate 0: 準備 **必須工程(スキップ不可)**

この仕様書の実行には `/dev:spec-run` スキルを使用すること。

**Gate 0 通過条件**: `/dev:spec-run` の実行プロトコルに従い、実行モード（従来 / Codex）を選択済みであること。

---

## 概要

[1-2 文で何を達成するか]

## 背景

[なぜこの変更が必要か、設計判断の理由]

## 設計決定事項

[確定した意思決定を表形式で列挙]

| #   | トピック | 決定 | 根拠 |
| --- | -------- | ---- | ---- |
| 1   | ...      | ...  | ...  |

## アーキテクチャ詳細

[データフロー図、構造定義、仕様値など実装に必要な詳細]
[ASCII 図、JSON スキーマ、型定義などを含める]

## 変更対象ファイルと影響範囲

### 変更するファイル

| ファイル | 変更内容 | 影響 |
| -------- | -------- | ---- |

### 新規作成ファイル

| ファイル | 内容 |
| -------- | ---- |

### 変更しないファイル

| ファイル | 理由 |
| -------- | ---- |

## 参照すべきファイル

実装着手前に必ず読むこと。

### コードベース内

| ファイル | 目的 |
| -------- | ---- |

### 参照資料（docs/PLAN/{YYMMDD}\_{slug}/references/ にコピー済み）

| ファイル | 目的 |
| -------- | ---- |

[外部参照がない場合はこのサブセクション自体を省略]

## タスクリスト

### 依存関係図

[Gate 単位で依存関係を図示]
```

Gate A: {フェーズ名}
├── Todo 1
└── Todo 2

Gate B: {フェーズ名}（Gate A 完了後）
└── Todo 3

```

### Gate N: {フェーズ名}

#### Todo N: {タイトル}

- [ ] **Step 1 — IMPL**
  - **対象**: {ファイルパス}
  - **内容**: {何を — 具体的なアクション}
  - **実装詳細**: {どうやって — 既存パターン参照、コード例}
  - **ワークフロー**: TDD / E2E / TASK
  - **依存**: {先行タスク — なければ「なし」}

- [ ] **Step 2 — Review {GateID}{N}**

**Gate N 通過条件**: 全 Review 結果記入欄が埋まり、総合判定が PASS であること

## 残存リスク
| リスク | 影響 | 緩和策 |
|--------|------|--------|
```

---

## ワークフロー分類

各タスクの `ワークフロー` フィールドには以下のいずれかを指定する:

| ワークフロー | 対象                                               | 判定基準                                       |
| ------------ | -------------------------------------------------- | ---------------------------------------------- |
| **TDD**      | ビジネスロジック、バリデーション、計算、データ変換 | 入出力が明確に定義でき、アサーションで検証可能 |
| **E2E**      | UI コンポーネント、レイアウト、ユーザー操作        | 視覚的確認または UX/UI 判断が必要              |
| **TASK**     | 設定ファイル、環境構築、ドキュメント               | テスト不要、コマンド実行結果で検証可能         |

判定フロー:

```
入出力が明確に定義できる？
    ├─ YES → TDD
    └─ NO → 視覚的確認が必要？
              ├─ YES → E2E
              └─ NO → TASK
```
